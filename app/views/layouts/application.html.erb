<!DOCTYPE html>
<html>
  <head>
    <title>Findr</title>
    <%= stylesheet_link_tag 'application', media: 'all',
                                           'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->
  </head>
  <body>
    <header class="navbar navbar-inverse">
      <div class="container">
        <nav>
          
          <ul class="nav navbar-nav navbar-left">
            <h3> Findr </h3>
          </ul>
            <ul class="nav navbar-nav navbar-right">
              
            <li><%= link_to "Home", root_path, :class => 'btn btn-default btn-sm' %></li>
            <% if logged_in? %>
            <li><%= link_to "Log out", logout_path, method: "delete", :class => 'btn btn-default btn-sm' %></li>
            <% else %>
            <li><%= link_to "Log in", '/login',:class => 'btn btn-default btn-sm' %></li>
            <li><%= link_to "Sign Up", '/signup',:class => 'btn btn-default btn-sm' %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
   
    <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
